<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Posts tagged #{{tag}} · {{ blog.name }}</title>
    </head>
    <body>
        <h1>Posts tagged #{{tag}} · {{ blog.name }}</h1>
        {{#each posts}}
            <section>
                <h1><a href="../../{{ this.slug }}">{{ this.title }}</a></h1>
                <div>{{ this.prettyDate }}</div>
                <div>{{#each this.tags}}<a href="../../tag/{{this}}">#{{this}}</a> {{/each}}</div>
                {{{ this.content }}}
                <hr>
            </section>
        {{else}}
            <p>No posts here yet, please come back later!</p>
        {{/each}}
    </body>
</html>
